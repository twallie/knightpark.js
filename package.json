{
  "name": "knightpark",
  "module": "src/module.ts",
  "type": "module",
  "types": "src/types.d.ts",
  "version": "0.1.0",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-plugin-dts": "^0.2.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun run build.mjs",
    "test": "bun run vitest"
  }
}
